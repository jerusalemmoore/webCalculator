let windowVal =document.getElementById('windowVal').innerHTML;
window.console.log(windowVal);
function addToEntry(digit){
  let digitToAdd = parseInt(digit);
  window.console.log(digitToAdd);
  if(digitToAdd == 0){
    windowVal = digitToAdd;
    window.console.log(windowVal);}
  else{

  }
}
